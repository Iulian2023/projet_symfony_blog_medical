{% extends "themes/base_admin.html.twig" %}

{% block title %} Modifier catégorie{% endblock %}

{% block description %}Modifier catégorie permet du modifier un catégorie existante{% endblock %}

{% block main %}
    <h1 class="text-center display-5 my-3">Modifier catégorie</h1>

    <div class="container mt-5">
        <div class=" bg-primary-subtle col-md-6 mx-auto rounded shadow p-5">
            {{ form_start(form) }}
                <div class="mb-3">
                    <label for="category_form_name" class="form-label">Nom : </label>
                    <div class="text-danger">
                        {{ form_errors(form.name) }}
                    </div>
                    {{ form_widget(form.name, {'attr' : {'class' : 'form-control'}}) }}
                </div>
                <div class="mb-3">
                    <input formnovalidate type="submit" value="Modifier" class="btn btn-primary w-100">
                </div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}