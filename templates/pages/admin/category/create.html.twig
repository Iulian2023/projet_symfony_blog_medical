{% extends "themes/base_admin.html.twig" %}

{% block title %} Nouvelle catégorie{% endblock %}

{% block description %}Nouvelle catégorie permet du crée un nouvelle catégorie{% endblock %}

{% block main %}
    <h1 class="text-center display-5 my-3">Nouvelle catégorie</h1>

    <div class="container mt-5">
        <div class=" bg-primary-subtle col-md-6 mx-auto rounded shadow p-5">
            {{ form_start(form) }}
                <div class="mb-3">
                    <label for="category_form_name" class="form-label">Nom : </label>
                    <div class="text-danger">
                        {{ form_errors(form.name) }}
                    </div>
                    {{ form_widget(form.name, {'attr' : {'class' : 'form-control'}}) }}
                </div>
                <div class="mb-3">
                    <input formnovalidate type="submit" value="Créer" class="btn btn-primary w-100">
                </div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}